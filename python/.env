# Which image to run from `docker compose`
IMAGE_TAG=v1.4.0

# Host port to expose FastAPI (FastAPI listens on 8000 in the container)
HOST_PORT=8080

# Host directory to persist data (mapped to /srv/budget-manager in the container)
# For local dev, you might prefer: DATA_DIR=./partition
DATA_DIR=/srv/budget-manager

